# kind-config.yml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # This maps port 30007 on your local machine (localhost)
  # to port 30007 on the kind "node" (which is a Docker container)
  extraPortMappings:
  - containerPort: 30007
    hostPort: 30007